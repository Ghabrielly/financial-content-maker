{
  "name": "financial-content-maker-frontend",
  "version": "1.0.0",
  "description": "Frontend para geração de conteúdo financeiro",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFiles": ["<rootDir>/test/setupTests.js"]
  }
}
